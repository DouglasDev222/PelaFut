services:
  pelafut:
    image: nginx:alpine
    container_name: pelafut
    ports:
      - "8090:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
